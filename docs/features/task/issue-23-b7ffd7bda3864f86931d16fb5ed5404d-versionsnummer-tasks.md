# Tasks: Versionsnummer (Issue #23)

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | Build | In `EmberMediaManager/My Project/AssemblyInfo.vb` `AssemblyVersion`/`AssemblyFileVersion` (Z. 34–35) von `1.11.1.0` auf `0.0.0.0` senken und die Platzhalterzeile `<Assembly: AssemblyInformationalVersion("")>` nach `AssemblyFileVersion` einfügen | Offen | — |
| 2 | Build | In `EmberAPI/My Project/AssemblyInfo.vb` `AssemblyVersion`/`AssemblyFileVersion` (Z. 34–35) von `1.11.1.0` auf `0.0.0.0` senken und die Platzhalterzeile `<Assembly: AssemblyInformationalVersion("")>` nach `AssemblyFileVersion` einfügen | Offen | — |
| 3 | Build | Neuen Schritt „Stamp assembly version" in `.github/actions/build-and-package/action.yml` zwischen „Restore dependencies" und „Build (Release x64)" einfügen (`shell: pwsh`, Input `release-version` via `env`; kein Rück-Commit ins Repo) | Offen | — |
| 4 | Validierung | Im „Stamp assembly version"-Schritt `release-version` normalisieren (führendes `v` defensiv entfernen, `-replace '^v',''`) und bei gesetztem Wert gegen das SemVer-Muster `X.Y.Z` bzw. `X.Y.Z-rc.N` validieren — bewusst strenger als `VERSION_PATTERN` in `resolve-release-version.mjs` (andere Pre-Release-Suffixe wie `-beta.1` schlagen laut fehl); bei Verletzung den Build mit Fehlermeldung abbrechen, die das akzeptierte Format nennt | Offen | — |
| 5 | Build | Im „Stamp assembly version"-Schritt Assembly-Version berechnen (`X.Y.Z` → `X.Y.Z.0`, `X.Y.Z-rc.N` → `X.Y.Z.N`) und Informational-Version (voller SemVer-String) bestimmen | Offen | — |
| 6 | Build | Im „Stamp assembly version"-Schritt die Attribute `AssemblyVersion`, `AssemblyFileVersion`, `AssemblyInformationalVersion` in `EmberMediaManager/My Project/AssemblyInfo.vb` per Regex ersetzen und gestempelte Werte ins Log schreiben | Offen | — |
| 7 | Build | Im „Stamp assembly version"-Schritt dieselben drei Attribute in `EmberAPI/My Project/AssemblyInfo.vb` per Regex ersetzen | Offen | — |
| 8 | Build | Im „Stamp assembly version"-Schritt bei leerem `release-version`-Input ohne Dateiänderung erfolgreich enden (Log „skipping stamp") | Offen | — |
| 9 | Validierung | Schritt „Verify publish output" in `action.yml` erweitern: bei gesetztem `release-version` für `publish/Ember Media Manager.exe` `FileVersionInfo.FileVersion` und `[Reflection.AssemblyName]::GetAssemblyName('publish\Ember Media Manager.exe').Version` (= `AssemblyVersion`, Quelle der angezeigten Version `My.Application.Info.Version`/`Master.Version`) gegen die erwartete Assembly-Version sowie `FileVersionInfo.ProductVersion` gegen den vollen SemVer-String prüfen, Abweichung → `throw` | Offen | — |
| 10 | Validierung | Im selben Verify-Schritt für `publish/EmberAPI.dll` `FileVersionInfo.FileVersion` und `AssemblyVersion` gegen die erwartete Assembly-Version prüfen, Abweichung → `throw` | Offen | — |
| 11 | Verifikation | Im nächsten Pre-Release-/Release-Lauf das Build-Log des Stempel-Schritts sowie `FileVersion`/`AssemblyVersion`/`ProductVersion` der ausgelieferten Exe und `FileVersion`/`AssemblyVersion` von `EmberAPI.dll` gegen den GitHub-Release-Tag abgleichen (dabei Nebenwirkung leeres `ProductVersion` bei Nicht-Release-Builds kurz verifizieren) | Offen | — |
