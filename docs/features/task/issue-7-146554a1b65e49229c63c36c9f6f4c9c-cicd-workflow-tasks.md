# Tasks: CI/CD-Workflow (Issue #7)

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | Paketupdate | `Newtonsoft.Json` `12.0.3` → `13.0.3` in `Addons/scraper.Trakttv.Data/packages.config` | Erledigt | Kein direkter Test — indirekt abgesichert durch Gate 2 (`security-scan`-Action/`pre-push`-Hook): `nuget restore` würde bei verbliebenem NU190x-Befund fehlschlagen |
| 2 | Paketupdate | `Newtonsoft.Json` `12.0.3` → `13.0.3` in `Addons/generic.Interface.Trakttv/packages.config` | Erledigt | Kein direkter Test — indirekt abgesichert durch Gate 2 (`security-scan`-Action/`pre-push`-Hook) |
| 3 | Paketupdate | `Newtonsoft.Json` `13.0.1` → `13.0.3` in den übrigen `packages.config` (`KodiAPI`, `EmberMediaManager`, `EmberAPI`, `Addons/scraper.TMDB.Trailer`, `Addons/scraper.TMDB.Poster`, `Addons/scraper.TMDB.Data`, `Addons/scraper.Data.OMDb`) | Erledigt | Kein direkter Test — indirekt abgesichert durch Gate 2 (`security-scan`-Action/`pre-push`-Hook) |
| 4 | Paketupdate | `<Reference>`-Version `12.0.0.0` → `13.0.0.0` + `<HintPath>` → `Newtonsoft.Json.13.0.3` in `scraper.Data.Trakttv.vbproj` und `generic.Interface.Trakttv.vbproj` | Erledigt | Kein direkter Test — indirekt abgesichert durch `build-and-test`-Job (Release-x64-MSBuild schlägt bei unauflösbarer Referenz fehl) |
| 5 | Paketupdate | `<HintPath>` `Newtonsoft.Json.13.0.1` → `Newtonsoft.Json.13.0.3` in den übrigen 7 Projektdateien (`KodiAPI.csproj`, `EmberMediaManager.vbproj`, `EmberAPI.vbproj`, `scraper.Trailer.TMDB.vbproj`, `scraper.Image.TMDB.vbproj`, `scraper.Data.TMDB.vbproj`, `scraper.Data.OMDb.vbproj`) | Erledigt | Kein direkter Test — indirekt abgesichert durch `build-and-test`-Job (Release-x64-MSBuild) |
| 6 | Paketupdate | Binding-Redirect `oldVersion="0.0.0.0-12.0.0.0" newVersion="12.0.0.0"` → `…-13.0.0.0`/`13.0.0.0` in `scraper.Trakttv.Data/app.config` und `generic.Interface.Trakttv/app.config`; per Grep verifizieren, dass kein weiterer Redirect auf `12.0.0.0` zeigt | Erledigt | Kein direkter Test — Grep-Verifikation in Review-Lauf 2 wiederholt: kein Redirect zeigt mehr auf `12.0.0.0` |
| 7 | Paketupdate | Lokal verifizieren: `nuget restore` ohne NU190x-Warnung + `msbuild Release\|x64` erfolgreich | Erledigt | `nuget restore` in Review-Lauf 2 erneut ausgeführt: befundfrei (keine NU190x-Warnungen, Vulnerability-Feed abgerufen); endgültiger MSBuild-Nachweis über CI-Job `build-and-test` (Task 31) |
| 8 | GitHooks | `.gitattributes` um `.githooks/* text eol=lf` inkl. Referenz-Kommentarblock ergänzen (dritte deklarierte Ausnahme der Additiv-Regel; im selben Commit wie die `.githooks/`-Dateien — wirkt beim Checkout auf LF) | Erledigt | Kein direkter Test — verifiziert in Review-Lauf 2: Eintrag in Commit `ad92cdc` zusammen mit `.githooks/`; Hooks mit Modus 100755 eingecheckt |
| 9 | GitHooks | `.githooks/install-hooks.cmd` anlegen (unverändert aus Referenz) | Erledigt | Kein direkter Test |
| 10 | GitHooks | `.githooks/install-hooks.sh` anlegen (unverändert aus Referenz) | Erledigt | Kein direkter Test |
| 11 | GitHooks | `.githooks/translation-check.py` aus Referenz übernehmen (Localizer-Scan + .resx-Konsistenz + Header-Validierung) | Erledigt | `python .githooks/translation-check.py --all` in Review-Lauf 2 erneut ausgeführt: befundfrei (Exit 0, 142 .resx) — Verbatim-Übernahme entspricht dem festgelegten Entscheidungspfad |
| 12 | GitHooks | `.githooks/pre-commit` anlegen (ruft `translation-check.py`) | Erledigt | Kein direkter Test — Python-Fallback (`python`/`python3`/`py -3`) in Iteration 2 ergänzt und verifiziert |
| 13 | GitHooks | `.githooks/pre-push` anlegen (`nuget restore` + NU190x-Auswertung, **blockierend** bei Befund — Spiegel des blockierenden Gates) | Erledigt | Kein direkter Test — Exit-1-Pfade bei Restore-Fehler und NU190x-Befund im Skript vorhanden; Muster in Iteration 2 auf `NU190[1-4]` präzisiert; Plattform-Erkennung (Windows direkt / `mono` / graceful skip) aus Commit `4e39a1ff` in Review-Lauf 3 verifiziert — blockierende Semantik bei ausführbarer Toolchain unverändert |
| 14 | GitHooks | Hooks lokal verifizieren: `python .githooks/translation-check.py --all` ausführen; befundfrei → `install-hooks.cmd` aktivieren; bei `.resx`-Bestandsbefunden → Befundliste dokumentieren und Skript eingrenzen, dann `install-hooks.cmd` + Probe-Commit | Erledigt | `translation-check.py --all` befundfrei (Review-Lauf 2, Exit 0) → Skript bleibt zu Recht verbatim; `core.hooksPath = .githooks` im lokalen Klon gesetzt |
| 15 | Composite Actions | `.github/actions/security-scan/action.yml` anlegen (`nuget restore` + NU190x-Parsing + Log-Artefakt; Inputs `solution-path`, `artifact-name`, `fail-on-vulnerabilities` mit Default `'true'`) | Erledigt | Kein direkter Test — Live-Nachweis über Gate-Job `static-checks` (Task 31); NU190[1-4]-Präzisierung aus Iteration 2 verifiziert |
| 16 | Composite Actions | `.github/actions/build-and-package/action.yml` anlegen (MSBuild `Release\|x64` → `publish/` → `version.json` → `release.zip` + `update.json`; kein NSIS-Installer) | Erledigt | Kein direkter Test — Live-Nachweis über `prerelease`-/`release`-Jobs (Task 31) |
| 17 | Release-Tooling | `package.json` anlegen (semantic-release-Dependencies aus Referenz, `name` angepasst) | Erledigt | Kein direkter Test — `npm ci` im `version`-Job (Task 31) |
| 18 | Release-Tooling | `package-lock.json` aus Referenz kopieren und `name`-Felder an `package.json` angleichen | Erledigt | Kein direkter Test — `name`-Felder stimmen mit `package.json` überein (`ember-media-manager`); `npm ci` im `version`-Job (Task 31) |
| 19 | Release-Tooling | `release.config.js` anlegen (`branches: ["master"]`, Plugin-Set wie Referenz) | Erledigt | Kein direkter Test — semantic-release-Dry-Run im `version`-Job (Task 31); „staging->master"-Kommentar in Iteration 2 korrigiert |
| 20 | Release-Tooling | `scripts/resolve-release-version.mjs` anlegen (`AUTOMATIC_RELEASE_BRANCHES = ["master"]`, Assets `release.zip` + `update.json`) | Erledigt | Kein direkter Test — Ausführung im `release`-Job (Task 31); „main"-Kommentare in Iteration 2 auf „master" korrigiert |
| 21 | Workflows | `.github/workflows/pr-staging-ci.yml` anlegen (`master`-Mapping, `nuget`/`msbuild`-Toolchain, Gates 2+3 mit `fail-on-vulnerabilities: 'true'`, Release-x64-Build-Job) | Erledigt | Kein direkter Test — Live-Verifikation via PR-Lauf (Task 31) |
| 22 | Workflows | `.github/workflows/staging-ci.yml` anlegen (Anzeigename „Pre-Release"; gleiche Gates inkl. blockierendem Security-Scan + `version`-/`prerelease`-Jobs) | Erledigt | Kein direkter Test — Live-Verifikation via „Pre-Release"-Lauf (Task 31) |
| 23 | Workflows | `.github/workflows/verify-pr-source.yml` anlegen (PRs gegen `master` nur von `staging`) | Erledigt | Kein direkter Test — Live-Verifikation bei erstem PR gegen `master` (Task 31); env-Indirektion für `github.head_ref` aus Iteration 2 verifiziert |
| 24 | Workflows | `.github/workflows/sync-staging-with-main.yml` anlegen (Back-Merge-PR `master` → `staging`) | Erledigt | Kein direkter Test — Live-Verifikation nach erstem Push auf `master` (Task 31); Anzeigename „Backmerge Master to Staging" in Iteration 2 korrigiert |
| 25 | Workflows | `.github/workflows/staging-to-main-promotion.yml` anlegen (Draft-PR `staging` → `master` nach „Pre-Release"-Erfolg) | Erledigt | Kein direkter Test — Live-Verifikation nach erstem „Pre-Release"-Erfolg (Task 31); Anzeigename „Promote staging to master" in Iteration 2 korrigiert; `workflow_run` referenziert weiterhin korrekt „Pre-Release" |
| 26 | Workflows | `.github/workflows/security-scan.yml` anlegen (wöchentlicher Cron + `workflow_dispatch`, `fail-on-vulnerabilities: 'true'`) | Erledigt | Kein direkter Test — `workflow_dispatch`-Lauf nach Merge auf `master` (Task 31); `permissions: contents: read` in Iteration 2 ergänzt |
| 27 | Workflows | `.github/workflows/release.yml` anlegen (Trigger `master`/`v*.*.*`; Release-Gate = Restore + Debug-Build; drei Release-Pfade wie Referenz) | Erledigt | Kein direkter Test — Live-Verifikation bei erstem Release (Task 31) |
| 28 | Doku | README-Abschnitt „Continuous Integration / Git Hooks" ergänzen (Hook-Aktivierung, Workflow-Überblick) — deklarierte Ausnahme der Additiv-Regel | Erledigt | Kein direkter Test |
| 29 | GitHub-Konfiguration | Seed-Tag `v1.12.0` auf `master`-Tip setzen und nach `origin` pushen — **vor dem ersten Merge der Workflow-Dateien auf `staging`** (der Merge triggert den ersten „Pre-Release"-Lauf; Tag zeigt auf alten Tip ohne `release.yml` → kein Release-Lauf durch den Tag-Push) | Offen | — |
| 30 | GitHub-Konfiguration | Branch-Protection für `staging`/`master` einrichten + Actions-Permissions prüfen (manuell, außerhalb Repo) | Offen | — |
| 31 | Verifikation | Live-Verifikation: PR gegen `staging` öffnen, Workflow-Lauf und Gate-Verhalten prüfen (Gate 2 befundfrei/grün); nach Merge „Pre-Release"/Promotion-Kette beobachten (erwartete RC-Version auf Basis `v1.12.0`) | Offen | — |
